// Operating hours display — owner: Nikao (task N4)
// Display-only. System NEVER rejects orders based on time (rule L04).

// TODO (N4): fetch from operating_hours table, render formatted schedule
export default function OperatingHours() {
  return <div>Operating hours — Mon–Fri 09:00–17:00, Sun 07:00–15:00</div>;
}
