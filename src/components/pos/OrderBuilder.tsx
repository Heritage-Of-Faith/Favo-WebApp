// Order builder — owner: Mine (task M3)
// Menu items + customisations. Submits via createOrder action.

// TODO (M3): implement menu grid + mod selector + order summary
export default function OrderBuilder() {
  return <div>OrderBuilder — task M3</div>;
}
