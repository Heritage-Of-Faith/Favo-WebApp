// Online/offline indicator — owner: Mine (task M7)

// TODO (M7): navigator.onLine + event listeners, show status chip
export default function OnlineIndicator() {
  return <div>OnlineIndicator — task M7</div>;
}
