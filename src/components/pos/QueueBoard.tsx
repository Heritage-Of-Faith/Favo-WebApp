// Live POS queue board — owner: Mine (task M5)
// SSE consumer via useOrderStream hook. Docs: docs/API.md → QueueEvent

// TODO (M5): render live order cards, update on SSE events
export default function QueueBoard() {
  return <div>QueueBoard — task M5</div>;
}
