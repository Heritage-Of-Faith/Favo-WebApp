// Staff list table — owner: Mia (task A4)
// shadcn Table. Docs: docs/DESIGN.md → Admin Rules

export type Props = { onEdit: (staffId: string) => void };

// TODO (A4): fetch staff list, render shadcn Table, edit via Dialog
export default function StaffTable(_props: Props) {
  return <div>StaffTable — task A4</div>;
}
