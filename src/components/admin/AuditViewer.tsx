// Audit log viewer — owner: Mia (task A6)
// Paginated, read-only. shadcn Table. Docs: docs/DATA_MODEL.md → audit_log

// TODO (A6): paginated fetch from audit_log, filter by entity_kind/actor
export default function AuditViewer() {
  return <div>AuditViewer — task A6</div>;
}
