// Menu item price editor — owner: Mia (task A5)
// Price edits must go through setMenuItemPrice action → price_history row.
// Docs: docs/API.md → setMenuItemPrice

// TODO (A5): editable price cells, shadcn Dialog for confirm, Sonner for toasts
export default function MenuEditor() {
  return <div>MenuEditor — task A5</div>;
}
