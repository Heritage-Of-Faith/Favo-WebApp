// TODO (G7): VAPID setup using web-push library
// Env vars: VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, NEXT_PUBLIC_VAPID_PUBLIC_KEY
// Docs: docs/API.md → POST /api/push/subscribe

export function initVapid(): void {
  throw new Error("Not implemented — see task G7");
}
