// Audit log viewer — owner: Mia (task A6)
// Paginated. Read-only. Docs: docs/DATA_MODEL.md → audit_log

export default function AuditPage() {
  return (
    <main>
      {/* TODO (A6): AuditViewer component — paginated audit_log display */}
      <p>Audit log (task A6)</p>
    </main>
  );
}
