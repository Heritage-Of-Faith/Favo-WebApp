// Admin dashboard shell — owner: Mia (task A2)
// HOFMI SSO auth gate. Docs: docs/DESIGN.md → Admin Rules

export default function AdminPage() {
  return (
    <main>
      {/* TODO (A2): Admin shell with Sidebar + auth gate */}
      <p>Admin dashboard (task A2)</p>
    </main>
  );
}
