// POS entry — owner: Mine (task M1)
// PIN login → order builder → queue. iPad portrait 768×1024.
// Docs: docs/DESIGN.md → POS Rules

export default function POSPage() {
  return (
    <main>
      {/* TODO (M1): LoginForm component — PIN pad, 4–6 digits, masked, large keys */}
      <p>POS — PIN Login (task M1)</p>
    </main>
  );
}
