// Customisations seed — task G3
// Extra shot, size options, milk alternatives.

import { db } from "../index";
import { menuCustomisations } from "../schema";

export async function seedCustomisations() {
  // TODO (G3): insert extra shot (R10), almond milk (R8), oat milk (R8), large size (R5)
  console.log("  → customisations (TODO G3)");
  void db;
  void menuCustomisations;
}
