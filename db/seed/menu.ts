// Menu seed — task G3
// Coffee items, prices, recipes. Docs: docs/DATA_MODEL.md

import { db } from "../index";
import { menuItems } from "../schema";

export async function seedMenu() {
  // TODO (G3): insert espresso, americano, cappuccino, flat white, latte, cold brew, etc.
  // currentPriceZar in integer cents (e.g. 4500 = R45.00)
  console.log("  → menu items (TODO G3)");
  void db;
  void menuItems;
}
